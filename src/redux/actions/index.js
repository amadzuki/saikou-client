import { authenticate, deauthenticate, register } from './auth'
import {
  setUser,
  resetUser,
  toggleFavoriteAnime,
  toggleFavoriteManga,
} from './user'

export {
  register,
  authenticate,
  deauthenticate,
  setUser,
  resetUser,
  toggleFavoriteAnime,
  toggleFavoriteManga,
}
